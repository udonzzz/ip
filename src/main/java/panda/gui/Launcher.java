package panda.gui;

import javafx.application.Application;

/**
 * Launcher is the entry point for the application for running the GUI.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
